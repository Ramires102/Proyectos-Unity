using UnityEngine;

public class Bate_sobol : MonoBehaviour
{
    public Animator anim_bate;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
